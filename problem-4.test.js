const largestPalindormeProduct = require('./problem-4');

describe('largestPalindormeProduct', () => {
  test('original problem test', () => {
    expect(largestPalindormeProduct()).toBe(906609);
  });
});
